import styled from 'styled-components';

const Card = styled.div`
  display: block;
  box-shadow: rgba(0, 0, 0, 0.1) 0px 4px 12px;
  border-radius: 20px;
  padding: 2em;
  background: white;
`;

export default Card;
