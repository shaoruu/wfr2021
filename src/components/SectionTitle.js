import styled from 'styled-components';

import { THEME_COLOR_0 } from '../config';

const SectionTitle = styled.h2`
  border: 0;
  border-left-width: 4px;
  border-color: ${THEME_COLOR_0}88;
  border-style: solid;
  padding-left: 0.6em;
  color: ${THEME_COLOR_0};
`;

export default SectionTitle;
